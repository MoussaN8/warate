export interface ThemeCategory {
  id: string;
  nom: string;
  nomWolof: string;
  icon: string;       // Nom de l'icône PrimeNG (ex: 'pi-star')
  colorClass: string; // Style Tailwind pour le badge d'icône
}